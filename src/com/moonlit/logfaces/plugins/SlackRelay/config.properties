# this web hook url to your slack service will be called by default when plugin is invoked
slack.url = https://hooks.slack.com/services/my/slack/webhook

# this slack chjannel will be used by defaul when plugin is invoked
slack.channel = #lfs