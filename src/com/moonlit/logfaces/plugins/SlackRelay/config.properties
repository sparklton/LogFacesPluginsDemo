# this url will used by default when plugin is invoked
slack.url = https://hooks.slack.com/services/my/slack/webhook

# this slack channel will be used by default when plugin is invoked
slack.channel = #lfs